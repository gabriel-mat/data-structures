#include <stdio.h>

int main()
{
    /*
     * Stacks:
     *
     *  A stack is a list with the restriction that both insertion and deletion
     * can be performed only from *one end*, called the *top* ;
     *
     * Last in, first out (LIFO)
     *
     * Common operations: push(), pop(), getTop(), isEmpty(),
     *
     * Applications:
     *  * function calls / recursion;
     *  * undo.
     */

    return 0;
}