#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
     *
     *   Queue - FIFO
     *
     *  => insertion must happen from one end (tail)
     *  => removal must happen from other end (head)
     *
     * " a list / collection with the restriction that insertion
     * can be performed at one end (rear / tail) and deletion can
     * be performed at other end (front / head)"
     *
     *   Common operations:
     *   - EnQueue(x) or Push(x)
     *   - Dequeue() or Pop()
     *   - Front() or Peek()
     *   - IsEmpty()
     *   - IsFull()
     *
     *    ! all of them must be O(1)
     * 
     */

    return 0;
}